1.start with class='container form-container'
2.fully center class='row justifiy-content-center'
3. mobile responseive class='col-md-6'
4.card class='card form-card'
5.form-header class='card-header form-header'
6.form-body class='form-body card-body'
7.<div>then label and input</div>
8.form end class='mt-3 form-end" including buttons
9.close form

js and php function

url()->previous() for back    javasccript:history.back()

offsetWidth = what you see
scroll width =total width


ALTER TABLE bookings
ADD COLUMN user_id BIGINT UNSIGNED after venue_id,
ADD CONSTRAINT fk_bookings_user
    FOREIGN KEY (user_id) REFERENCES users(id)
    ON DELETE CASCADE;
